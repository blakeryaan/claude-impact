// SDG tags use the brand's restrained palette — no rainbow.
// JetBrains Mono uppercase, muted border/text on paper background.
export function sdgChipClass(_sdg: string): string {
  return 'bg-transparent border border-muted text-muted';
}
